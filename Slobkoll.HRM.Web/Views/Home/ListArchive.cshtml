
<table class="table table-hover table-inverse">
    <caption class="showarchive">Архив задач <span>-</span></caption>
    <tbody class="higarchive">
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    <div class="row">
                        <div class="col-md-2 col-xs-2 text-center">
                            <img src="~/Content/1.png" class="img-responsive center-block" width="30" height="30" />
                        </div>
                        <p id="@item.Id" class="id">Задача № @item.Id</p>
                        <div class="col-md-12 col-xs-12">
                            <p>@item.Name</p>
                            <div class="row">
                                <div class="col-md-6 col-xs-6">
                                    <span class="left">@item.Status</span>
                                </div>
                                <div class="col-md-6 col-xs-6">
                                    <span class="right">@item.Username</span>
                                </div>
                            </div>
                        </div>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>