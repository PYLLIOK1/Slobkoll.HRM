@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutHome.cshtml";
}

<div class="row">
    <div class="col-md-4 col-xs-12 block-left">
        @{Html.RenderAction("ListAuthor", new { id = ViewBag.Id });}
        @{Html.RenderAction("ListPerfomer", new { id = ViewBag.Id });}
        @{Html.RenderAction("ListObserver", new { id = ViewBag.Id });}
        @{Html.RenderAction("ListArchive", new { id = ViewBag.Id });}
        @{Html.RenderAction("ListObserverArchive", new { id = ViewBag.Id });}
    </div>
    <div id="workspace" class="col-md-8 col-xs-12 block-right">

    </div>
</div>
